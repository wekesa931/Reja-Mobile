import * as React from 'react';
import { Text } from 'react-native';

export interface RevenueGraphProps {
    
}
 
const ExpenseGraph = () => {
    return <Text>Expense Graph</Text>
}
 
export default ExpenseGraph;